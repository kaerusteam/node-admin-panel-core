{
	"name": "<%=moduleName%>",
	"version": "0.1.0",
	"main": "<%=basename%>.js",
	"exports": {
		"urlPrefix": "<%=basename%>",
		"methods": [
			{
				"name": "create",
				"handler": "create<%=moduleName%>",
				"options": {
					"needAuth": true
				}
			},
			{
				"name": "update",
				"handler": "update<%=moduleName%>",
				"options": {
					"needAuth": true
				}
			},
			{
				"name": "read",
				"handler": "read<%=moduleName%>",
				"options": {
					"needAuth": true
				}
			},
			{
				"name": "delete",
				"handler": "delete<%=moduleName%>",
				"options": {
					"needAuth": true
				}
			},
			{
				"name": "readList",
				"handler": "readList<%=moduleName%>",
				"options": {
					"needAuth": true
				}
			}
		],
		"schema" : {
			"create<%=moduleName%>" : {
				"type" : "object",
				"properties" : {
					
				},
				"required" : []
			},
			"update<%=moduleName%>" : {
				"type" : "object",
				"properties" : {
					"_id" : {
						"type" : "string",						
						"pattern" : "^\\w{12,24}$"
					}
				},
				"required" : ["_id"]
			},
			"read<%=moduleName%>" : {
				"type" : "object",
				"properties" : {
					"_id" : {
						"type" : "string",
						"pattern" : "^\\w{12,24}$"
					}
				},
				"required" : ["_id"]
			},
			"delete<%=moduleName%>" : {
				"type" : "object",
				"properties" : {
					"_id" : {
						"type" : "string",
						"pattern" : "^\\w{12,24}$"
					}
				},
				"required" : ["_id"]
			},
			"readList<%=moduleName%>" : {
				"type" : "object",
				"properties" : {
					
				},
				"required" : []
			}
		}
	}
}